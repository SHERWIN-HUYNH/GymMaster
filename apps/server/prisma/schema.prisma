datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}


model user{
    id String @id @default(cuid())
    name String
    email String @unique
    password String 
     @@map("user")
}