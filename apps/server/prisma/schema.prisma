datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")

}

generator client {
  provider = "prisma-client-js"
}


model user{
    id String @id @default(cuid())
    name String
    email String @unique
    password String 
     @@map("user")
}